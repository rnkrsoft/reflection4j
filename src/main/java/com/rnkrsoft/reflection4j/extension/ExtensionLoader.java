/**
 * Apache License
 * Version 2.0, January 2004
 * http://www.apache.org/licenses/
 * <p>
 * TERMS AND CONDITIONS FOR USE, REPRODUCTION, AND DISTRIBUTION
 * <p>
 * 1. Definitions.
 * <p>
 * "License" shall mean the terms and conditions for use, reproduction,
 * and distribution as defined by Sections 1 through 9 of this document.
 * <p>
 * "Licensor" shall mean the copyright owner or entity authorized by
 * the copyright owner that is granting the License.
 * <p>
 * "Legal Entity" shall mean the union of the acting entity and all
 * other entities that control, are controlled by, or are under common
 * control with that entity. For the purposes of this definition,
 * "control" means (i) the power, direct or indirect, to cause the
 * direction or management of such entity, whether by contract or
 * otherwise, or (ii) ownership of fifty percent (50%) or more of the
 * outstanding shares, or (iii) beneficial ownership of such entity.
 * <p>
 * "You" (or "Your") shall mean an individual or Legal Entity
 * exercising permissions granted by this License.
 * <p>
 * "Source" form shall mean the preferred form for making modifications,
 * including but not limited to software source code, documentation
 * source, and configuration files.
 * <p>
 * "Object" form shall mean any form resulting from mechanical
 * transformation or translation of a Source form, including but
 * not limited to compiled object code, generated documentation,
 * and conversions to other media types.
 * <p>
 * "Work" shall mean the work of authorship, whether in Source or
 * Object form, made available under the License, as indicated by a
 * copyright notice that is included in or attached to the work
 * (an example is provided in the Appendix below).
 * <p>
 * "Derivative Works" shall mean any work, whether in Source or Object
 * form, that is based on (or derived from) the Work and for which the
 * editorial revisions, annotations, elaborations, or other modifications
 * represent, as a whole, an original work of authorship. For the purposes
 * of this License, Derivative Works shall not include works that remain
 * separable from, or merely link (or bind by name) to the interfaces of,
 * the Work and Derivative Works thereof.
 * <p>
 * "Contribution" shall mean any work of authorship, including
 * the original version of the Work and any modifications or additions
 * to that Work or Derivative Works thereof, that is intentionally
 * submitted to Licensor for inclusion in the Work by the copyright owner
 * or by an individual or Legal Entity authorized to submit on behalf of
 * the copyright owner. For the purposes of this definition, "submitted"
 * means any form of electronic, verbal, or written communication sent
 * to the Licensor or its representatives, including but not limited to
 * communication on electronic mailing lists, source code control systems,
 * and issue tracking systems that are managed by, or on behalf of, the
 * Licensor for the purpose of discussing and improving the Work, but
 * excluding communication that is conspicuously marked or otherwise
 * designated in writing by the copyright owner as "Not a Contribution."
 * <p>
 * "Contributor" shall mean Licensor and any individual or Legal Entity
 * on behalf of whom a Contribution has been received by Licensor and
 * subsequently incorporated within the Work.
 * <p>
 * 2. Grant of Copyright License. Subject to the terms and conditions of
 * this License, each Contributor hereby grants to You a perpetual,
 * worldwide, non-exclusive, no-charge, royalty-free, irrevocable
 * copyright license to reproduce, prepare Derivative Works of,
 * publicly display, publicly perform, sublicense, and distribute the
 * Work and such Derivative Works in Source or Object form.
 * <p>
 * 3. Grant of Patent License. Subject to the terms and conditions of
 * this License, each Contributor hereby grants to You a perpetual,
 * worldwide, non-exclusive, no-charge, royalty-free, irrevocable
 * (except as stated in this section) patent license to make, have made,
 * use, offer to sell, sell, import, and otherwise transfer the Work,
 * where such license applies only to those patent claims licensable
 * by such Contributor that are necessarily infringed by their
 * Contribution(s) alone or by combination of their Contribution(s)
 * with the Work to which such Contribution(s) was submitted. If You
 * institute patent litigation against any entity (including a
 * cross-claim or counterclaim in a lawsuit) alleging that the Work
 * or a Contribution incorporated within the Work constitutes direct
 * or contributory patent infringement, then any patent licenses
 * granted to You under this License for that Work shall terminate
 * as of the date such litigation is filed.
 * <p>
 * 4. Redistribution. You may reproduce and distribute copies of the
 * Work or Derivative Works thereof in any medium, with or without
 * modifications, and in Source or Object form, provided that You
 * meet the following conditions:
 * <p>
 * (a) You must give any other recipients of the Work or
 * Derivative Works a copy of this License; and
 * <p>
 * (b) You must cause any modified files to carry prominent notices
 * stating that You changed the files; and
 * <p>
 * (c) You must retain, in the Source form of any Derivative Works
 * that You distribute, all copyright, patent, trademark, and
 * attribution notices from the Source form of the Work,
 * excluding those notices that do not pertain to any part of
 * the Derivative Works; and
 * <p>
 * (d) If the Work includes a "NOTICE" text file as part of its
 * distribution, then any Derivative Works that You distribute must
 * include a readable copy of the attribution notices contained
 * within such NOTICE file, excluding those notices that do not
 * pertain to any part of the Derivative Works, in at least one
 * of the following places: within a NOTICE text file distributed
 * as part of the Derivative Works; within the Source form or
 * documentation, if provided along with the Derivative Works; or,
 * within a display generated by the Derivative Works, if and
 * wherever such third-party notices normally appear. The contents
 * of the NOTICE file are for informational purposes only and
 * do not modify the License. You may add Your own attribution
 * notices within Derivative Works that You distribute, alongside
 * or as an addendum to the NOTICE text from the Work, provided
 * that such additional attribution notices cannot be construed
 * as modifying the License.
 * <p>
 * You may add Your own copyright statement to Your modifications and
 * may provide additional or different license terms and conditions
 * for use, reproduction, or distribution of Your modifications, or
 * for any such Derivative Works as a whole, provided Your use,
 * reproduction, and distribution of the Work otherwise complies with
 * the conditions stated in this License.
 * <p>
 * 5. Submission of Contributions. Unless You explicitly state otherwise,
 * any Contribution intentionally submitted for inclusion in the Work
 * by You to the Licensor shall be under the terms and conditions of
 * this License, without any additional terms or conditions.
 * Notwithstanding the above, nothing herein shall supersede or modify
 * the terms of any separate license agreement you may have executed
 * with Licensor regarding such Contributions.
 * <p>
 * 6. Trademarks. This License does not grant permission to use the trade
 * names, trademarks, service marks, or product names of the Licensor,
 * except as required for reasonable and customary use in describing the
 * origin of the Work and reproducing the content of the NOTICE file.
 * <p>
 * 7. Disclaimer of Warranty. Unless required by applicable law or
 * agreed to in writing, Licensor provides the Work (and each
 * Contributor provides its Contributions) on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or
 * implied, including, without limitation, any warranties or conditions
 * of TITLE, NON-INFRINGEMENT, MERCHANTABILITY, or FITNESS FOR A
 * PARTICULAR PURPOSE. You are solely responsible for determining the
 * appropriateness of using or redistributing the Work and assume any
 * risks associated with Your exercise of permissions under this License.
 * <p>
 * 8. Limitation of Liability. In no event and under no legal theory,
 * whether in tort (including negligence), contract, or otherwise,
 * unless required by applicable law (such as deliberate and grossly
 * negligent acts) or agreed to in writing, shall any Contributor be
 * liable to You for damages, including any direct, indirect, special,
 * incidental, or consequential damages of any character arising as a
 * result of this License or out of the use or inability to use the
 * Work (including but not limited to damages for loss of goodwill,
 * work stoppage, computer failure or malfunction, or any and all
 * other commercial damages or losses), even if such Contributor
 * has been advised of the possibility of such damages.
 * <p>
 * 9. Accepting Warranty or Additional Liability. While redistributing
 * the Work or Derivative Works thereof, You may choose to offer,
 * and charge a fee for, acceptance of support, warranty, indemnity,
 * or other liability obligations and/or rights consistent with this
 * License. However, in accepting such obligations, You may act only
 * on Your own behalf and on Your sole responsibility, not on behalf
 * of any other Contributor, and only if You agree to indemnify,
 * defend, and hold each Contributor harmless for any liability
 * incurred by, or claims asserted against, such Contributor by reason
 * of your accepting any such warranty or additional liability.
 * <p>
 * END OF TERMS AND CONDITIONS
 * <p>
 * APPENDIX: How to apply the Apache License to your work.
 * <p>
 * To apply the Apache License to your work, attach the following
 * boilerplate notice, with the fields enclosed by brackets "[]"
 * replaced with your own identifying information. (Don't include
 * the brackets!)  The text should be enclosed in the appropriate
 * comment syntax for the file format. We also recommend that a
 * file or class name and description of purpose be included on the
 * same "printed page" as the copyright notice for easier
 * identification within third-party archives.
 * <p>
 * Copyright 2018 rnkrsoft
 * <p>
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 * <p>
 * http://www.apache.org/licenses/LICENSE-2.0
 * <p>
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
package com.rnkrsoft.reflection4j.extension;

import com.rnkrsoft.utils.StringUtils;
import lombok.extern.slf4j.Slf4j;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.ConcurrentMap;
import java.util.regex.Pattern;

/**
 * Created by rnkrsoft.com on 2019/8/23.
 * 此类参照Dubbo中的ExtensionLoader
 */
@Slf4j
public class ExtensionLoader<T> implements IExtensionLoader<T> {
    private static final String EXTENSIONS_DIRECTORY = "META-INF/extensions/";
    //存放拓展点接口类和拓展加载器实例的映射，用于防止重构创建加载器
    private static final ConcurrentMap<Class<?>, ExtensionLoader<?>> EXTENSION_LOADERS = new ConcurrentHashMap();
    //存放拓展点实现类和实例之间的映射，用于加速单例的获得
    private static final ConcurrentMap<Class<?>, Object> EXTENSION_INSTANCES = new ConcurrentHashMap();
    //存放拓展点接口类与拓展点之间的映射，用于已加载的实例
    private static final ConcurrentMap<Class<?>, String> EXTENSION_GLOBAL_INSTANCES = new ConcurrentHashMap();
    //名称提供逗号分割
    private static final Pattern NAME_SEPARATOR = Pattern.compile("\\s*[,]+\\s*");

    static <T> boolean isWithExtensionAnnotation(Class<T> type) {
        return type.isAnnotationPresent(SPI.class);
    }

    /**
     * 获取指定接口的拓展加载器
     *
     * @param type 接口类
     * @param <T>  拓展点实例类型
     * @return 拓展加载器
     */
    @SuppressWarnings("unchecked")
    public static <T> ExtensionLoader<T> getExtensionLoader(Class<T> type) {
        if (type == null) {
            throw new IllegalArgumentException("Extension type is null");
        }
        if (!type.isInterface()) {
            throw new IllegalArgumentException("Extension type(" + type + ") is not interface!");
        }
        if (!isWithExtensionAnnotation(type)) {
            throw new IllegalArgumentException("Extension type(" + type + ") is not extension, because WITHOUT @" + SPI.class.getSimpleName() + " Annotation!");
        }
        ExtensionLoader<T> loader = (ExtensionLoader<T>) EXTENSION_LOADERS.get(type);
        if (loader == null) {
            EXTENSION_LOADERS.putIfAbsent(type, new ExtensionLoader<T>(type));
            loader = (ExtensionLoader<T>) EXTENSION_LOADERS.get(type);
        }
        return loader;
    }

    /**
     * 拓展点类接口
     */
    final Class<?> type;
    //服务接口默认的拓展点
    private String cachedDefaultName;
    private Map<Class<? extends T>, Integer> cachedWrapperClasses;
    //已加载的拓展点名称和实例之间的映射关系，注意此为单例
    private final ConcurrentMap<String, Holder<Object>> cachedInstances = new ConcurrentHashMap<String, Holder<Object>>();
    //将拓展点名称和异常缓存起来
    private final ConcurrentMap<String, IllegalStateException> exceptions = new ConcurrentHashMap<String, IllegalStateException>();
    //映射拓展点名称和实现类之间的映射关系
    private final Holder<Map<String, Class<?>>> cachedClasses = new Holder();
    //映射实现类和拓展点名称之间的映射关系
    private final ConcurrentMap<Class<?>, String> cachedNames = new ConcurrentHashMap();

    /**
     * 构建一个接口服务对应的加载器实例
     *
     * @param type 接口服务
     */
    private ExtensionLoader(Class<?> type) {
        this.type = type;
    }

    public T getExtensionByClassName(String className) {
        getExtensionClasses();
        String extensionName = getExtensionNameByClassName(className);
        return getExtension(extensionName);
    }

    public T newExtension(String name) {
        return createExtension(name, false);
    }


    public T getExtension() {
        return getExtension(null);
    }

    public T getGlobalExtension(final String name) {
        String name0 = name;
        T instance;
        if (EXTENSION_GLOBAL_INSTANCES.containsKey(type)) {
            name0 = EXTENSION_GLOBAL_INSTANCES.get(type);
        } else {
            if (!hasExtension(name0)) {
                throw new IllegalArgumentException("Extension instance(name: " + name + ", class: " + type + ")  could not be instantiated");
            }
            EXTENSION_GLOBAL_INSTANCES.put(type, name0);
        }
        instance = getExtension(name0);
        return instance;
    }

    public T getExtension(String name) {
        if (name == null || name.length() == 0) {
            return getDefaultExtension();
        }
        Holder<Object> holder = cachedInstances.get(name);
        if (holder == null) {
            cachedInstances.putIfAbsent(name, new Holder<Object>());
            holder = cachedInstances.get(name);
        }
        Object instance = holder.get();
        if (instance == null) {
            synchronized (holder) {
                instance = holder.get();
                if (instance == null) {
                    instance = createExtension(name, true);
                    holder.set(instance);
                }
            }
        }
        return (T) instance;
    }


    public T getDefaultExtension() {
        //扫描所有拓展点类
        getExtensionClasses();
        if (null == cachedDefaultName || cachedDefaultName.isEmpty()) {
            return null;
        } else {
            return getExtension(cachedDefaultName);
        }
    }

    public T getLoadedExtension(String name) {
        if (name == null || name.length() == 0) {
            throw new IllegalArgumentException("Extension name is null");
        }
        Holder<Object> holder = cachedInstances.get(name);
        if (holder == null) {
            cachedInstances.putIfAbsent(name, new Holder<Object>());
            holder = cachedInstances.get(name);
        }
        return (T) holder.get();
    }

    public Set<String> getLoadedExtensions() {
        return Collections.unmodifiableSet(new TreeSet<String>(cachedInstances.keySet()));
    }

    public Set<String> getSupportedExtensions() {
        Map<String, Class<?>> classes = getExtensionClasses();
        return Collections.unmodifiableSet(new TreeSet<String>(classes.keySet()));
    }



    public String getExtensionNameByClassName(String className) {
        if (className == null || className.length() == 0) {
            throw new IllegalArgumentException("Extension className is null");
        }
        getExtensionClasses();
        for (Map.Entry<Class<?>, String> entry : cachedNames.entrySet()) {
            if (entry.getKey().getName().equals(className)) {
                return entry.getValue();
            }
        }
        return cachedDefaultName;
    }

    public Class<?> getExtensionClass(String name) {
        if (type == null) {
            throw new IllegalArgumentException("Extension type is null");
        }
        if (name == null) {
            throw new IllegalArgumentException("Extension name is null");
        }
        Class<?> clazz = getExtensionClasses().get(name);
        if (clazz == null) {
            throw new IllegalStateException("No such extension \"" + name + "\" for " + type.getName() + "!");
        }
        return clazz;
    }

    public boolean hasExtension(String name) {
        if (name == null || name.length() == 0) {
            throw new IllegalArgumentException("Extension name is null");
        }
        try {
            return getExtensionClass(name) != null;
        } catch (Throwable t) {
            return false;
        }
    }

    /**
     * 创建指定拓展点实现的拓展实例
     *
     * @param name           拓展点名称
     * @param singleInstance 是否单例
     * @return 拓展点的实例
     */
    T createExtension(String name, boolean singleInstance) {
        Class<?> implClass = getExtensionClasses().get(name);
        if (implClass == null) {
            throw createNotFoundException(name);
        }
        try {
            T instance = null;
            //如果是单例
            if (singleInstance) {
                instance = (T) EXTENSION_INSTANCES.get(implClass);
                if (instance == null) {
                    EXTENSION_INSTANCES.putIfAbsent(implClass, (T) implClass.newInstance());
                    instance = (T) EXTENSION_INSTANCES.get(implClass);
                }
            } else {
                instance = (T) implClass.newInstance();
            }
            Map<Class<? extends T>, Integer> wrapperClasses = cachedWrapperClasses;
            if (wrapperClasses != null && !wrapperClasses.isEmpty()) {
                List<Map.Entry<Class<? extends T>, Integer>> sortEntries = new ArrayList(wrapperClasses.entrySet());
                Collections.sort(sortEntries, new Comparator<Map.Entry<Class<? extends T>, Integer>>() {
                    @Override
                    public int compare(Map.Entry<Class<? extends T>, Integer> o1, Map.Entry<Class<? extends T>, Integer> o2) {
                        if (o1.getValue() == o2.getValue()) {
                            return 0;
                        }
                        if (o1.getValue() > o2.getValue()) {
                            return 1;
                        } else {
                            return -1;
                        }
                    }
                });
                for (Map.Entry<Class<? extends T>, Integer> entry : sortEntries) {
                    Class<? extends T> wrapperClass = entry.getKey();
                    instance = (T) wrapperClass.getConstructor(type).newInstance(instance);
                }
            }
            return instance;
        } catch (Throwable t) {
            throw new IllegalStateException("Extension instance(name: " + name + ", class: " + type + ")  could not be instantiated: " + t.getMessage(), t);
        }
    }

    /**
     * 创建未发现异常
     *
     * @param name 拓展点名称
     * @return 异常对象
     */
    IllegalStateException createNotFoundException(String name) {
        for (Map.Entry<String, IllegalStateException> entry : exceptions.entrySet()) {
            if (entry.getKey().toLowerCase().contains(name.toLowerCase())) {
                return entry.getValue();
            }
        }
        StringBuilder buf = new StringBuilder("No such extension " + type.getName() + " by name " + name);

        int i = 1;
        for (Map.Entry<String, IllegalStateException> entry : exceptions.entrySet()) {
            if (i == 1) {
                buf.append(", possible causes: ");
            }

            buf.append("\r\n(");
            buf.append(i++);
            buf.append(") ");
            buf.append(entry.getKey());
            buf.append(":\r\n");
            buf.append(StringUtils.toString(entry.getValue()));
        }
        return new IllegalStateException(buf.toString());
    }


    /**
     * 加载所有的扩展点的类
     *
     * @return 所有拓展点类集合
     */
    Map<String, Class<?>> getExtensionClasses() {
        Map<String, Class<?>> classes = cachedClasses.get();
        if (classes == null) {
            synchronized (cachedClasses) {
                classes = cachedClasses.get();
                if (classes == null) {
                    classes = loadExtensionClasses();
                    cachedClasses.set(classes);
                }
            }
        }
        return classes;
    }

    /**
     * 加载当前拓展点的所有实现类，不可被外部调用
     *
     * @return 实现类键值对
     */
    Map<String, Class<?>> loadExtensionClasses() {
        //读取拓展接口上标注的@SPI，读取默认拓展点名称
        final SPI defaultAnnotation = type.getAnnotation(SPI.class);
        if (defaultAnnotation != null) {
            String value = defaultAnnotation.value();
            if (value != null && (value = value.trim()).length() > 0) {
                String[] names = NAME_SEPARATOR.split(value);
                if (names.length > 1) {
                    throw new IllegalStateException("more than 1 default extension name on extension " + type.getName() + ": " + Arrays.toString(names));
                }
                if (names.length == 1) {
                    cachedDefaultName = names[0];
                }
            }
        }
        final Map<String, Class<?>> extensionClasses = new HashMap();
        loadFile(extensionClasses, EXTENSIONS_DIRECTORY);
        return extensionClasses;
    }

    /**
     * 在指定路径下加载服务接口的配置文件
     *
     * @param extensionClasses 拓展类集合
     * @param dir
     */
    @SuppressWarnings("unchecked")
    void loadFile(final Map<String, Class<?>> extensionClasses, String dir) {
        //META-INF/extensions/xxx.xxxx.xxx
        String fileName = dir + type.getName();
        try {
            Enumeration<java.net.URL> urls;
            //查找加载器,如果没找到，使用系统加载器
            ClassLoader classLoader = findClassLoader();
            if (classLoader != null) {
                urls = classLoader.getResources(fileName);
            } else {
                urls = ClassLoader.getSystemResources(fileName);
            }
            if (urls != null) {
                while (urls.hasMoreElements()) {
                    java.net.URL url = urls.nextElement();
                    //读取查找到的文件
                    try {
                        BufferedReader reader = new BufferedReader(new InputStreamReader(url.openStream(), "utf-8"));
                        try {
                            String line = null;
                            String className = null;
                            while ((line = reader.readLine()) != null) {
                                //如果有#内容，则为注释，将保留#号前的内容
                                //形式如下
                                //javassits=com.rnkrsoft.reflection4j.extension.compiler.JavassistCompiler#这是一个测试
                                final int ci = line.indexOf('#');
                                if (ci >= 0) {
                                    line = line.substring(0, ci);
                                }
                                //将内容前后的空白字符去除掉
                                line = line.trim();
                                className = line;
                                if (line.length() > 0) {
                                    try {
                                        String name = null;
                                        //按照=号进行拆分
                                        int i = line.indexOf('=');
                                        if (i > 0) {
                                            name = line.substring(0, i).trim();
                                            className = line.substring(i + 1).trim();
                                        }
                                        if (className.length() > 0) {
                                            Class<? extends T> clazz = (Class<? extends T>) Class.forName(className, true, classLoader);
                                            //如果加载的类与服务接口不适于实现关系，则抛出异常
                                            if (!type.isAssignableFrom(clazz)) {
                                                throw new IllegalStateException("Error when load extension class(interface: " + type + ", class: " + clazz.getName() + "), class " + clazz.getName() + "is not subtype of interface.");
                                            }
                                            //如果META-INF/extensions/xxx.xxxx.xxx中只定义了类全限定名，则需要实现类标注@Extension，用于申明拓展点名称
                                            if (name == null) {
                                                Extension extensionAnnotation = clazz.getAnnotation(Extension.class);
                                                if (extensionAnnotation == null) {
                                                    if (extensionAnnotation == null) {
                                                        throw new IllegalArgumentException("\"" + clazz + "\" is not extension, because neither META-INF/extensions/" + type.getName() + " define extension name or without @" + Extension.class.getSimpleName() + " Annotation!");
                                                    }
                                                }
                                                name = extensionAnnotation.value();
                                            }
                                            //检查服务实现是否使用服务接口构建，如果存在则构建为包装，绝大多数都会发生异常
                                            try {
                                                clazz.getConstructor(type);
                                                Wrapper wrapperAnnotation = clazz.getAnnotation(Wrapper.class);
                                                if (wrapperAnnotation == null) {
                                                    throw new IllegalArgumentException("\"" + clazz + "\" is not extension, because WITHOUT @" + Wrapper.class.getSimpleName() + " Annotation!");
                                                }
                                                Map<Class<? extends T>, Integer> wrappers = cachedWrapperClasses;
                                                if (wrappers == null) {
                                                    cachedWrapperClasses = new ConcurrentHashMap();
                                                    wrappers = cachedWrapperClasses;
                                                }
                                                wrappers.put(clazz, wrapperAnnotation.priority());
                                            } catch (NoSuchMethodException e) {
                                                //不会对该异常进行处理，仅仅是用于识别类的构造函数属于什么类型
                                                //获取无参构造函数，如果成功，则对进行处理
                                                clazz.getConstructor();
                                                String[] names = NAME_SEPARATOR.split(name);
                                                if (names != null && names.length > 0) {
                                                    for (String n : names) {
                                                        if (!cachedNames.containsKey(clazz)) {
                                                            cachedNames.put(clazz, n);
                                                        }
                                                        Class<?> c = extensionClasses.get(n);
                                                        if (c == null) {
                                                            extensionClasses.put(n, clazz);
                                                        } else if (c != clazz) {
                                                            throw new IllegalStateException("Duplicate extension " + type.getName() + " name " + n + " on " + c.getName() + " and " + clazz.getName());
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    } catch (Throwable t) {
                                        IllegalStateException e = new IllegalStateException("Failed to load extension class(interface: " + type + ", class: " + line + ") in " + url + ", cause: " + t.getMessage(), t);
                                        exceptions.put(line, e);
                                    }
                                }
                            } // end of while read lines
                        } finally {
                            reader.close();
                        }
                    } catch (Throwable t) {
                        log.error("Exception when load extension class(interface: " + type + ", class file: " + url + ") in " + url, t);
                    }
                } // end of while urls
            }
        } catch (Throwable t) {
            log.error("Exception when load extension class(interface: " + type + ", description file: " + fileName + ").", t);
        }
    }

    private ClassLoader findClassLoader() {
        return ExtensionLoader.class.getClassLoader();
    }
    //---------------------------------------以下为静态内部类----------------------------------------------------

    /**
     * 持有对象
     *
     * @param <T>
     */
    public static class Holder<T> {

        private volatile T value;

        public void set(T value) {
            this.value = value;
        }

        public T get() {
            return value;
        }

    }

    public static class ConcurrentHashSet<E> extends AbstractSet<E> implements Set<E>, java.io.Serializable {

        private static final long serialVersionUID = -8672117787651310382L;

        private static final Object PRESENT = new Object();

        private final ConcurrentHashMap<E, Object> map;

        public ConcurrentHashSet() {
            map = new ConcurrentHashMap<E, Object>();
        }

        public ConcurrentHashSet(int initialCapacity) {
            map = new ConcurrentHashMap<E, Object>(initialCapacity);
        }

        public Iterator<E> iterator() {
            return map.keySet().iterator();
        }

        public int size() {
            return map.size();
        }

        public boolean isEmpty() {
            return map.isEmpty();
        }

        public boolean contains(Object o) {
            return map.containsKey(o);
        }

        public boolean add(E e) {
            return map.put(e, PRESENT) == null;
        }

        public boolean remove(Object o) {
            return map.remove(o) == PRESENT;
        }

        public void clear() {
            map.clear();
        }

    }
}
